{
  "name": "deltamod-community-cli",
  "version": "1.1.0-beta.1",
  "description": "Safe project tooling for Deltamod Community mod packages",
  "license": "ISC",
  "author": "GhinoRhino - Deltamodders",
  "type": "commonjs",
  "main": "index.js",
  "bin": {
    "deltamod-community": "index.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "check": "node scripts/check.js",
    "build": "npm run check && esbuild index.js --bundle --platform=node --target=node22 --outfile=dist/bundle.js && node scripts/build-sea.js"
  },
  "dependencies": {
    "archiver": "^8.0.0",
    "chalk": "^4.1.2",
    "fast-xml-parser": "^5.10.1",
    "js-toml": "^1.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "postject": "^1.0.0-alpha.6"
  }
}
